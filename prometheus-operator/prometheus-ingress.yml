apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  name: "prom-operator-prometheus"
  namespace: "monitoring"
spec:
  ingressClassName: "nginx"
  rules:
    - host: "prometheus.k3s"
      http:
        paths:
          - path: "/"
            pathType: "Prefix"
            backend:
              service:
                name: "prom-operator-kube-prometh-prometheus"
                port:
                  number: 9090
